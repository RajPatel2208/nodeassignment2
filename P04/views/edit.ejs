<!DOCTYPE html>
<html>
<head>
  <title>Edit Employee</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2>Edit Employee</h2>
    <form method="POST" action="/edit/<%= emp._id %>">
      <label for="name" class="form-label">Name</label>
      <input id="name" name="name" class="form-control" value="<%= emp.name %>" required>

      <label for="email" class="form-label mt-2">Email</label>
      <input id="email" name="email" class="form-control" value="<%= emp.email %>" required>

      <label for="baseSalary" class="form-label mt-2">Base Salary</label>
      <input id="baseSalary" name="baseSalary" type="number" class="form-control" value="<%= emp.baseSalary %>" required>

      <button class="btn btn-warning mt-3">Update Employee</button>
    </form>
  </div>
</body>
</html>
